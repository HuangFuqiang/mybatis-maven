<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.code.mybatismaven.UserMapper">

    <insert id="insertRecode" parameterType="com.code.mybatismaven.User">
        insert into user(name) value(#{name})
    </insert>

    <select id="selectAll" resultType="com.code.mybatismaven.User">
        select * from user
    </select>
</mapper>